import React from "react";
import { Container, Heading, Text } from "@chakra-ui/react";

function signUp() {
  return (
    <Container>
      <Heading>Sign Up</Heading>
      <Text>Sign in | Sign up</Text>
    </Container>
    // Lägg till en ruta där du kan logga in. En där det står "log in" och en "sign up"
    // Två inputfält för "Användarnamn" & "Lösenord"
  );
}

// Showreel är för närvarande tom men ska hämta och lägga in en exempelvideo som spelas upp.
// Kan vara video för en viss produkt med.

export default signUp;
